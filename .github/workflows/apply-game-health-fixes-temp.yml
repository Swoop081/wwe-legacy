name: Apply Game Health Fixes Temp
on:
  push:
    branches: [main]
    paths: ['.github/workflows/apply-game-health-fixes-temp.yml']
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - name: Correct Giulia Momentum distribution
        run: python tools/apply-giulia-balance-fix-temp.py
      - name: Validate full systems
        shell: bash
        run: |
          node --check js/ai/WrestlingAI.js
          node tools/full-card-tier-audit.mjs
          node tools/architecture-audit-temp.mjs
          node tools/ai-action-usage-audit.mjs
          node tools/ai-choice-audit.mjs
      - name: Commit fixes
        run: |
          git config user.name 'Swoop081'
          git config user.email 'justinbelot8@gmail.com'
          git add js/data/decks.js
          git commit -m 'v1.1.199 correct Giulia playable Momentum balance'
          git push origin main
# exact roadmap assignment retry
