name: Becky authentic 60-card deck apply
on:
  push:
    paths: ['.github/becky-authentic-60-apply-trigger']
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {ref: main}
      - name: Apply and certify Becky five-tier curves
        run: python3 tools/becky-final-lock.py
      - name: Commit Becky five-tier lock
        run: |
          git config user.name 'WWE Legacy Build Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add js/data/content.js js/data/decks.js js/shared/v1.1.69-card-identity-pass.js js/data/reward-printings.js
          git diff --cached --check
          git commit -m 'Lock Becky Lynch full five-tier move curves'
          git push origin main
