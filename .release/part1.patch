diff --git a/js/ui/app.js b/js/ui/app.js
--- a/js/ui/app.js
+++ b/js/ui/app.js
@@ -385,7 +385,7 @@
 const BOOSTER_PACK_BRANDING = Object.freeze({
-  "summerslam-series-1": { label:"SUMMERSLAM", tone:"summer", logo: assetUrl("assets/images/art-summerslam-series-1-summerslam-2026-logo.png") },
+  "summerslam-series-1": { label:"SUMMERSLAM", tone:"summer", logo: "https://raw.githubusercontent.com/Swoop081/wwe-legacy/6f0c7b74a51112624f3172670bd8ca76c810971f/assets/images/art-summerslam-series-1-summerslam-2026-logo.png" },
@@ -412,8 +412,7 @@
       ${brand.logo ? `<img class="physical-booster-logo" src="${brand.logo}" alt="${brand.label} logo">` : `<strong class="physical-booster-wordmark">${brand.label}</strong>`}
       <b class="physical-booster-series">SERIES 1</b>
-      <span class="physical-booster-set-line">${set.name.toUpperCase()}</span>
-      <small>SERIES 1 · 4 CARDS + 1 MERCH · AMETHYST CHASE</small>
     </span>
   </span>`;
 }
@@ -1104,7 +1103,7 @@
     const reward = boosterRevealResults[boosterRevealIndex] ?? {};
     const p = reward?.pull ?? reward;
     const universePointsValue = Math.max(0, Math.floor(Number(p?.universePointsValue) || 0));
-    const converted = !!p?.universePointsCredited;
+    const converted = !!p?.universePointsCredited && universePointsValue > 0;
     const convertedMarkup = converted
       ? `<div class="single-card-reward-overlay"><small>DUPLICATE CONVERTED</small><strong>+${universePointsValue}</strong><b>UP</b><em>UNIVERSE POINTS</em></div>`
       : "";
@@ -1114,10 +1113,10 @@
       <div class="single-card-reveal-head"><span>CARD ${boosterRevealIndex + 1} OF ${boosterRevealResults.length}</span><div class="single-card-reveal-dots">${boosterRevealResults.map((_,i)=>`<i class="${i===boosterRevealIndex?'active':''}"></i>`).join("")}</div></div>
       <div class="single-card-reveal-stage ${converted ? 'is-converted' : ''}">
         <div class="single-card-reveal-glow"></div>
-        ${collectibleCardMarkup(p.card, { large: true, tier: p.tier, flipped: true, classes: "booster-reveal-card" })}
+        ${collectibleCardMarkup(p.card, { large: true, tier: p.tier, flipped: p.card?.kind === "merch" && !finishedCardArtFor(p.card), classes: "booster-reveal-card" })}
         ${convertedMarkup}
       </div>
       <div class="single-card-reveal-meta"><strong>${converted ? `+${universePointsValue} UP` : (p.tier ?? "normal").toUpperCase()}</strong>${converted ? '<span>DUPLICATE CONVERTED</span>' : `<span>${p.rarity ?? ""}</span>`}</div>
-      <p class="single-card-reveal-hint">TAP CARD · ${boosterRevealIndex >= boosterRevealResults.length - 1 ? 'PACK SUMMARY' : 'NEXT CARD'}</p>
+      <p class="single-card-reveal-hint">${boosterRevealIndex >= boosterRevealResults.length - 1 ? 'PACK SUMMARY' : 'NEXT CARD'}</p>
     </section>`;
@@ -1176,9 +1175,8 @@
     const packMarkup = physicalBoosterPackMarkup(boosterOpeningSet, { opening: false, wrapperState: "sealed" });
     root.innerHTML = `<section class="booster-screen booster-rip-screen">
       <div class="booster-rip-head"><span>PACK OPENING</span><strong>${boosterOpeningSet.name}</strong></div>
       <button id="booster-rip-pack" class="booster-rip-pack" aria-label="Rip open ${boosterOpeningSet.name}">${packMarkup}</button>
       <strong class="booster-rip-cta">TAP TO RIP</strong>
-      <p class="booster-rip-note">Open the pack and reveal all five cards</p>
     </section>`;
