diff --git a/js/ui/app.js b/js/ui/app.js
--- a/js/ui/app.js
+++ b/js/ui/app.js
@@ -1,6 +1,6 @@
 function liveFrontRaritySvg(card) {
   const rarity = Math.max(1, Math.min(4, Number(card?.rarity) || 1));
-  return Array.from({ length: rarity }, (_, i) => `<text class="ccg-live-rarity-star" x="51" y="${62 + i * 31}" text-anchor="middle" dominant-baseline="middle">★</text>`).join("");
+  return Array.from({ length: rarity }, (_, i) => `<text class="ccg-live-rarity-star" x="68" y="${78 + i * 31}" text-anchor="middle" dominant-baseline="middle">★</text>`).join("");
 }
 function liveFrontMethodDotsSvg(card) {
   if (!card || card.kind !== "move" || card.finisher) return "";
diff --git a/package.json b/package.json
--- a/package.json
+++ b/package.json
@@ -1,6 +1,6 @@
 {
   "name": "wwe-legacy-ccg",
-  "version": "1.1.98",
+  "version": "1.1.99",
   "private": true,
   "type": "module",
   "scripts": {
